<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\FraudEvaluation.Domain\FraudEvaluation.Domain.csproj" />
    <PackageReference Include="MediatR" Version="14.2.0" />
    <!-- MediatR.Extensions.Microsoft.DependencyInjection removed to avoid pulling an older MediatR dependency that conflicts with MediatR v14 -->
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
